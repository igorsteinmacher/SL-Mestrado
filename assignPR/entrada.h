#ifndef ENTRADA_H
#define	ENTRADA_H

#include <stdbool.h> // tipo bool
#include <ctype.h> // função isdigit

/* Protótipo de função */
int posicaoDoPonto(char[]);
bool posicaoSinalCorreta(char[]);
bool temMaisDeUmSinal(char[]);
bool temMaisDeUmPonto(char[]);
bool numStringEhValido(char[]);

#endif /* ENTRADA_H */

